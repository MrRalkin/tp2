import { ConvertirHeurePipe } from './convertir-heure.pipe';

describe('ConvertirHeurePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirHeurePipe();
    expect(pipe).toBeTruthy();
  });
});
